server:
  port: 8081
  servlet.context-path: /api-vulky
vulky:
  app:
    jwtSecret: VulkiKey123
    jwtExpirationMs: 86400000
spring:
  application:
    name: api-vulky
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: true
    database:
      url:loocal: